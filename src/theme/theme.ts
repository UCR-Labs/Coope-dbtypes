/**
 * Representa los colores de la aplicación a usar
 */
export interface Theme {
    /* color principal de las apps */
    primaryColor: string;
    
    /* color secundario de las apps */
    secondaryColor: string;
    
    /* color terciario de las apps */
    tertiaryColor: string;
    
    /* color para mensajes de problemas/prevención en las apps */
    warningColor: string;
    
    /* color para mensajes de exito en las apps */
    successColor: string;
    
    /* color para mensajes de error en las apps */
    errorColor: string;
  
  }
  